import styled from 'styled-components';
import col from './colors';

const UserItem = styled.div`
    padding: 20px;
    font-size: 20px;
    color: ${col[2]};
    box-shadow: 0 0 10px ${col[0]};
`;

export default UserItem;