import styled from 'styled-components';
import col from './colors';

const Button = styled.button`
    padding: 5px 20px;
    margin-bottom: 20px;
    border: 1px solid ${col[0]};
`;

export default Button;